# Adversarial tests

The suite that tries to **break the thesis**. This is the one that matters and the one
that goes in the README.

Coverage percentage is not a goal here — naming the specific failure each test prevents is.

| Test | Failure it prevents |
|---|---|
| | |
