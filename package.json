{
  "name": "trigsight",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:correctness": "vitest run tests/correctness",
    "test:adversarial": "vitest run tests/adversarial",
    "test": "vitest run",
    "verify:citations": "tsx bench/citations/verify.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
